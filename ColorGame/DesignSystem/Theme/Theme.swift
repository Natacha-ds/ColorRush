//
//  Theme.swift
//  ColorRush
//
//  Design system root namespace. Every visual constant consumed by the
//  redesigned UI lives under Theme.* — colors, spacing, radii, shadows,
//  gradients. Call sites must reference Theme.* and not inline literals.
//

import SwiftUI

enum Theme {}
